import { Component } from '@angular/core';
import { SectionTitle } from '../section-title/section-title';

@Component({
  selector: 'app-pro-title',
  imports: [SectionTitle],
  templateUrl: './pro-title.html',
  styleUrl: './pro-title.scss',
})
export class ProTitle {

}
