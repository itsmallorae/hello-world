import { Component } from '@angular/core';
import { SectionTitle } from '../section-title/section-title';

@Component({
  selector: 'app-def-title',
  imports: [SectionTitle],
  templateUrl: './def-title.html',
  styleUrl: './def-title.scss',
})
export class DefTitle {

}
